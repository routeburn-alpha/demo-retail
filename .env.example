# Provisioned by Neon (Vercel Marketplace). Pull with: vercel env pull .env.local
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# Shared password for the /admin page (added in milestone M3).
ADMIN_PASSWORD="change-me"
